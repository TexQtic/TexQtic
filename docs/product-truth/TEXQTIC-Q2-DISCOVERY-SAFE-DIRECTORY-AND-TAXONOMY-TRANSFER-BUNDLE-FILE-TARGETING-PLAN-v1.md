# TEXQTIC-Q2-DISCOVERY-SAFE-DIRECTORY-AND-TAXONOMY-TRANSFER-BUNDLE-FILE-TARGETING-PLAN-v1

Status: PRODUCT-TRUTH / BOUNDED PLANNING ARTIFACT ONLY
Date: 2026-04-14
Authority posture: three-artifact-inheriting, planning-only, Q2-targeting reference

## 1. Purpose and Authority

This artifact records one bounded Q2 file-targeting decision only.

Its purpose is narrow:

- identify the exact current owner file set for the remaining discovery-safe directory /
  taxonomy-transfer-prevention row bundle now open inside the Aggregator section of the tenant
  dashboard matrix
- determine which exact rows must move together so discovery-safe terminology drift,
  taxonomy-transfer drift, and implied ownership drift are removed without reopening Q1
- determine whether those rows are fully local to one descriptive surface and one grouped section
- define the smallest safe future implementation allowlist for a Q2-only descriptive correction
  pass
- record only the same-file adjacency rules and edge cases that materially affect whether this Q2
  row bundle may stay one-file-local

This artifact does not:

- authorize runtime implementation
- rename any row in this pass
- reopen Q1, Q4, WL, or any already closed line
- widen into runtime, shell, route, taxonomy implementation, provisioning, tenant-admin,
  entitlement, CRM, or marketing work
- claim closure of the entire older Q2 queue lineage outside the exact remaining row trio opened by
  this prompt
- replace the controlling anti-drift artifacts listed below

Usage rule:

- later Q2-facing work must inherit this artifact together with the controlling anti-drift
  artifacts listed below
- this artifact governs exact source ownership, sparse same-file bundle boundaries, same-file
  context, and the smallest safe future write boundary only

Authority order used:

1. Layer 0 posture
   - `governance/control/OPEN-SET.md`
   - `governance/control/NEXT-ACTION.md`
   - `governance/control/BLOCKED.md`
   - `governance/control/SNAPSHOT.md`
2. Locked governance architecture truth
   - `docs/governance/control/GOV-OS-001-DESIGN.md`
3. Controlling product-truth anti-drift references
   - `docs/product-truth/TEXQTIC-NAVIGATION-AND-LABEL-RECONCILIATION-PLAN-v1.md`
   - `docs/product-truth/TEXQTIC-SURFACE-TO-NORMALIZED-LABEL-APPLICATION-MAP-v1.md`
   - `docs/product-truth/TEXQTIC-VISIBLE-LABEL-IMPLEMENTATION-PREPARATION-QUEUE-v1.md`
4. Descriptive source surface
   - `docs/strategy/TENANT_DASHBOARD_MATRIX.md`

## 2. Scope Boundary

Included:

- exact-label tracing for the remaining Q2 row trio only
- file-level ownership of `Supplier Network`, `Buyer Network`, and `Catalog Hub`
- the smallest same-file adjacency review needed to determine which contextual elements must remain
  in scope so a later correction stays truthful
- minimal descriptive reuse checks needed to rule in or rule out a second descriptive owner file
- the smallest safe future implementation allowlist for a Q2-only descriptive correction pass

Excluded:

- Q1-corrected rows
- runtime implementation
- shell rewrite
- route rewrite
- broader dashboard rewrite beyond Q2 targeting
- full taxonomy-transfer lineage outside the exact current row trio

This artifact is the Q2 file-targeting plan only.

## 3. Inherited Controlling Truth

This artifact inherits, without reinterpretation, all of the following:

- Q1 is already closed
- Q2 is the next smallest lawful unit
- Aggregator may consume only a discovery-safe subset and must not imply full taxonomy ownership
  transfer
- discovery, profile, and catalog wording in Aggregator context must not imply supplier or
  manufacturer classification ownership or board-level operating authority
- the already normalized Aggregator heading and context paragraph remain preserved same-file truth
  unless a later row-local wording choice strictly requires bounded support
- this pass targets the exact remaining row trio inside the descriptive matrix and does not itself
  reopen the broader multi-surface queue lineage

## 4. Exact Evidence Trace

| File | Evidence class | Observed label or data | Q2 relevance | Targeting conclusion |
| --- | --- | --- | --- | --- |
| `docs/strategy/TENANT_DASHBOARD_MATRIX.md` | normalized section heading context | `### Aggregator workspace — Curated Directory & Intent Handoff` | preserves bounded workspace framing above the remaining Q2 rows | REQUIRED CONTEXT ONLY: existing heading may remain unchanged in later Q2 work |
| `docs/strategy/TENANT_DASHBOARD_MATRIX.md` | immediate explanatory context | Aggregator context paragraph states Aggregators are not primary owners of downstream transaction execution, settlement, or platform workflow orchestration | same-file truth anchor that continues to bound the section | REQUIRED CONTEXT ONLY: existing paragraph may remain unchanged unless a later row rewrite makes bounded supporting copy necessary |
| `docs/strategy/TENANT_DASHBOARD_MATRIX.md` | exact Q2 TA row | `Supplier Network` | discovery-safe terminology and taxonomy-transfer drift | IN SCOPE: exact remaining Q2 row |
| `docs/strategy/TENANT_DASHBOARD_MATRIX.md` | exact Q2 TA row | `Buyer Network` | discovery-safe terminology and implied ownership drift | IN SCOPE: exact remaining Q2 row |
| `docs/strategy/TENANT_DASHBOARD_MATRIX.md` | exact Q2 TS row | `Catalog Hub` | discovery/catalog ownership drift and taxonomy-transfer drift | IN SCOPE: exact remaining Q2 row |
| `docs/strategy/TENANT_DASHBOARD_MATRIX.md` | preserved adjacent Q1 rows | `Shared downstream order continuation`, `Qualified intent handoff`, `Downstream negotiation continuation`, `Historical finance overhang (non-canonical)`, `Downstream communication continuation` | corrected non-owning context already present between the TA and TS slices | PRESERVED ADJACENCY: must remain untouched in later Q2 work |
| `docs/strategy/TENANT_DASHBOARD_MATRIX.md` | nearby same-section rows | `Org Profile`, `Membership & RBAC`, `Compliance Dashboard`, `Pricing Governance`, `Approval Chains`, `Network Notifications`, `Invoice Management`, `Settlement Visibility` | adjacent rows inside the same Aggregator section | REVIEW-ONLY: not currently mapped into Q2 by the controlling queue/application map and must not be widened in by implication |
| `docs/**` exact-search result | descriptive reuse exclusion check | exact matches for `Supplier Network`, `Buyer Network`, and `Catalog Hub` as current row labels appear only in `docs/strategy/TENANT_DASHBOARD_MATRIX.md` outside planning lineage references | determines whether the row trio is local or multi-surface | NO SECOND DESCRIPTIVE OWNER FOUND for the exact current row labels |
| `docs/product-truth/TEXQTIC-VISIBLE-LABEL-IMPLEMENTATION-PREPARATION-QUEUE-v1.md` | queue-lineage constraint | broader Q2 lineage also names `Supplier / Manufacturer Capability Directory / Profiling` in a different product-truth surface | broader taxonomy-transfer lineage context | EDGE CASE ONLY: this later row-bundle pass must not overclaim whole-line closure outside the exact current row trio |

## 5. Q2 Targeting Findings

Exact current owner file set for the remaining Q2 discovery-safe row bundle:

- one descriptive file only: `docs/strategy/TENANT_DASHBOARD_MATRIX.md`

Exact current Q2 row trio inside that file:

- `Supplier Network`
- `Buyer Network`
- `Catalog Hub`

Classification result:

- these exact Q2 row labels are static, descriptive, and file-local
- these exact Q2 row labels are not owned by a second descriptive matrix
- these exact Q2 row labels remain within one grouped Aggregator section but are sparse rather than
  contiguous: two rows under TA and one row under TS
- these exact Q2 row labels must move together in a later pass because together they teach
  discovery-safe directory and catalog depth as if Aggregator owns supplier/buyer network
  governance or taxonomy-level product catalog authority
- the corrected Q1 rows between them now form preserved non-owning context rather than additional
  rename targets

Same-file adjacency result:

- the normalized Aggregator heading and its context paragraph are the required same-file envelope
  for later Q2 work
- the later Q2 pass must preserve the already corrected Q1 rows between the TA and TS slices
- no additional same-file heading rename is required by default for this Q2 row-bundle pass

## 6. Canonical Q2 File-Targeting Plan

Primary targeting conclusion:

- the exact current Q2 owner remains one descriptive source surface only:
  `docs/strategy/TENANT_DASHBOARD_MATRIX.md`

Smallest safe future implementation allowlist if this Q2 row bundle remains local:

- `docs/strategy/TENANT_DASHBOARD_MATRIX.md`

Required same-file row bundle that must move together in a later Q2-only correction pass:

- `Supplier Network`
- `Buyer Network`
- `Catalog Hub`

Required same-file context that must remain in scope during that later pass, but does not require
automatic rename by default:

- `### Aggregator workspace — Curated Directory & Intent Handoff`
- the immediate Aggregator context paragraph under that heading
- the already corrected Q1 rows that sit between the TA and TS slices

Explicitly excluded adjacent same-file rows:

- `Org Profile`
- `Membership & RBAC`
- `Compliance Dashboard`
- `Historical finance overhang (non-canonical)`
- `Invoice Management`
- `Settlement Visibility`
- `Pricing Governance`
- `Downstream communication continuation`
- `Approval Chains`
- `Network Notifications`

## 7. Local-Bundle Versus Widening Rules

This Q2 row bundle remains local when all of the following stay true:

- the future write remains descriptive-only
- the future write corrects only the exact three mapped rows inside the Aggregator section of
  `docs/strategy/TENANT_DASHBOARD_MATRIX.md`
- the future write keeps the normalized Aggregator heading and context paragraph as preserved
  section context
- the future write preserves the already corrected Q1 rows instead of reopening them

This Q2 row bundle may still remain one-file-local if a later pass adds a short bounded discovery-
safe or non-owning clarifier inside one or more of the three exact rows, because that remains row-
local descriptive work inside the same file.

This Q2 row bundle widens beyond its safe local boundary only if any of the following is
attempted:

- reopening the Q1 row bundle
- attempting to close the broader queue lineage item `Supplier / Manufacturer Capability Directory /
  Profiling` in the same pass
- reopening runtime shells, routes, or other surfaces by implication
- absorbing other adjacent Aggregator rows without a new targeting pass

## 8. Allowed and Disallowed Targeting Interpretations

Allowed:

- treating `docs/strategy/TENANT_DASHBOARD_MATRIX.md` as the exact current owner of the remaining
  Q2 row trio
- treating the exact Q2 bundle as the three mapped rows only
- treating the normalized Aggregator heading and context paragraph as preserved same-file context,
  not as active rename targets by default
- treating the already corrected Q1 rows as preserved adjacency rather than reopened targets

Disallowed:

- treating the broader queue-lineage mention of `Supplier / Manufacturer Capability Directory /
  Profiling` as proof that the exact current row trio is multi-file-owned
- absorbing Q1-corrected or other adjacent Aggregator rows into Q2 by implication alone
- treating this targeting artifact as authority to perform runtime or multi-file normalization now
- claiming whole-line taxonomy-transfer closure outside the exact remaining row trio opened here

## 9. Blockers and Edge Cases

- no blocker prevents this Q2 row trio from remaining a one-file-local descriptive bundle
- no second descriptive owner file was found for the exact current row labels `Supplier Network`,
  `Buyer Network`, and `Catalog Hub`
- one implementation edge case remains inside the later pass: the three rows are sparse rather than
  contiguous, so the future edit must change two TA rows and one TS row while preserving the
  already corrected Q1 rows between them
- one scope edge case remains from queue lineage: the broader Q2 family still mentions a separate
  `Supplier / Manufacturer Capability Directory / Profiling` item outside this exact row trio, so a
  later row-only correction must not overstate itself as full multi-surface taxonomy-transfer
  closure

## 10. Readiness Outcome

Result of this artifact:

- TexQtic now has one bounded Q2 file-targeting plan that identifies the exact remaining
  discovery-safe directory / taxonomy-transfer-prevention row bundle inside the Aggregator section
- this Q2 row trio is confirmed as one-file-local to `docs/strategy/TENANT_DASHBOARD_MATRIX.md`
- the later implementation allowlist is reduced to one descriptive file with one exact three-row
  sparse bundle
- the already corrected Q1 rows and the broader multi-surface taxonomy-transfer lineage are
  explicitly excluded from the future Q2-only correction pass
- no blocker forces runtime, schema, contract, or multi-surface widening in order to execute a
  later Q2 row-bundle correction pass
