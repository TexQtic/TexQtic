# TEXQTIC-Q6-TENANT-PLAN-METADATA-FILE-TARGETING-PLAN-v1

Status: PRODUCT-TRUTH / BOUNDED PLANNING ARTIFACT ONLY
Date: 2026-04-14
Authority posture: seven-artifact-inheriting, planning-only, Q6-isolation reference

## 1. Purpose and Authority

This artifact records one bounded Q6 file-targeting decision only.

Its purpose is narrow:

- identify the exact current source file set for the visible label `Tenant Plans`
- distinguish exact current label ownership from nearby live runtime plan-related labels
- determine whether Q6 remains static, derived, shared, reused, or already runtime-backed
- define the smallest safe later Q6-only implementation allowlist without widening into runtime,
  route, schema, or contract work

This artifact does not:

- authorize runtime implementation
- rename any label in this pass
- reopen the locked family, overlay, capability, package, or tenant-admin architecture
- merge Q6 into broader navigation, finance, billing, or tenant-detail normalization
- replace the seven inherited controlling product-truth artifacts

Usage rule:

- later Q6-facing work must inherit this artifact together with the seven controlling product-truth
  artifacts listed below
- this artifact governs exact current source ownership, adjacent-surface exclusion, and the
  smallest safe future write boundary only

Authority order used:

1. Layer 0 posture
   - `governance/control/OPEN-SET.md`
   - `governance/control/NEXT-ACTION.md`
   - `governance/control/BLOCKED.md`
   - `governance/control/SNAPSHOT.md`
2. Locked governance architecture truth
   - `docs/governance/control/GOV-OS-001-DESIGN.md`
3. Controlling product-truth anti-drift references
   - `docs/product-truth/TEXQTIC-RUNTIME-TAXONOMY-PROVISIONING-TENANT-ADMIN-CANONICAL-VOCABULARY-STATE-MAP-v1.md`
   - `docs/product-truth/TEXQTIC-PROVISIONING-ALIGNMENT-MATRIX-v1.md`
   - `docs/product-truth/TEXQTIC-TENANT-ADMIN-SURFACE-MATRIX-v1.md`
   - `docs/product-truth/TEXQTIC-TAXONOMY-PROPAGATION-AND-DISCOVERY-SAFE-EXPOSURE-MAP-v1.md`
   - `docs/product-truth/TEXQTIC-NAVIGATION-AND-LABEL-RECONCILIATION-PLAN-v1.md`
   - `docs/product-truth/TEXQTIC-SURFACE-TO-NORMALIZED-LABEL-APPLICATION-MAP-v1.md`
   - `docs/product-truth/TEXQTIC-VISIBLE-LABEL-IMPLEMENTATION-PREPARATION-QUEUE-v1.md`
4. Descriptive evidence surfaces only
   - `docs/DASHBOARD_MATRIX_CONTROL_TENANT_WL.md`
5. Runtime-facing exclusion checks only
   - `components/ControlPlane/TenantDetails.tsx`
   - `components/ControlPlane/TenantRegistry.tsx`
   - `layouts/SuperAdminShell.tsx`
   - `App.tsx`
   - `services/controlPlaneService.ts`
   - `types.ts`
   - `server/src/services/tenantProvision.service.ts`

## 2. Scope Boundary

Included:

- exact-label tracing for `Q6` only
- file-level ownership of the current visible label `Tenant Plans`
- nearest runtime-facing plan-label exclusion checks needed to determine whether Q6 is already live
- the smallest safe later write boundary for an isolated Q6-only pass

Excluded:

- runtime implementation
- finance tower redesign
- tenant deep-dive redesign
- shell/navigation relabeling
- data-model or API changes
- schema, migration, entitlement, or billing workflow changes
- grouped queue items `Q1` through `Q5`

This artifact is the Q6 file-targeting plan only.

## 3. Inherited Controlling Truth

This artifact inherits, without reinterpretation, all of the following:

- package/plan remains a separate commercial and entitlement axis
- control-plane tower labels may remain platform-supervision language without claiming full billing
  or entitlement completeness
- `Tenant plan metadata` or similar phrasing may remain package-visibility language without
  claiming billing-complete or family-defining authority
- `Q6` remains the only queue item previously identified as safe for isolated first-wave handling
- grouped family, overlay, Aggregator, and taxonomy contradiction bundles must not be reopened here

## 4. Exact Evidence Trace

| File | Evidence class | Observed label or data | Q6 relevance | Targeting conclusion |
| --- | --- | --- | --- | --- |
| `docs/DASHBOARD_MATRIX_CONTROL_TENANT_WL.md` | exact current visible-label owner | `Tenant Plans` Finance Tower row | exact Q6 source | IN SCOPE: this row owns the exact current label |
| `docs/DASHBOARD_MATRIX_CONTROL_TENANT_WL.md` | same-file derivative reference | `Tenant Plans + Fee Ledger stubs` priority summary | same-label summary carry-forward | IN SCOPE: this summary reference must stay aligned with the row label inside the same file |
| `components/ControlPlane/TenantDetails.tsx` | live runtime analog only | `Plan & Quotas` tab and `Billing` tab | nearby plan/billing UI wording | OUT OF SCOPE for Q6 exact-label correction: different static labels on a separate tenant deep-dive surface |
| `components/ControlPlane/TenantRegistry.tsx` | live runtime analog only | `Plan` table header and tenant plan value | nearby plan metadata display | OUT OF SCOPE for Q6 exact-label correction: different static label on registry table |
| `layouts/SuperAdminShell.tsx` | navigation framing only | `Tenants` and `Finance & Fees` | shell-level adjacent navigation | OUT OF SCOPE: no `Tenant Plans` owner or reuse |
| `App.tsx` | route wiring only | separate `tenant_registry`, `tenant_detail`, and `finance` route rendering | proves separation of runtime surfaces | OUT OF SCOPE: routing confirms no shared runtime label cluster |
| `services/controlPlaneService.ts` | data availability check | `Tenant.plan` in control-plane responses | confirms plan metadata already exists in frontend payloads | OUT OF SCOPE for current label correction; useful only to confirm no backend blocker |
| `types.ts` | normalization check | `normalizeCommercialPlan` and `TenantConfig.plan` | confirms existing plan normalization vocabulary | OUT OF SCOPE for current label correction |
| `server/src/services/tenantProvision.service.ts` | persistence check | `plan` selected and written on provisioning path | confirms package/plan data is already persisted | OUT OF SCOPE for current label correction |

## 5. File-Targeting Decision

Exact current source file set for the visible label `Tenant Plans`:

- one file only: `docs/DASHBOARD_MATRIX_CONTROL_TENANT_WL.md`

Exact current occurrence set inside that file:

- Finance Tower row label: `Tenant Plans`
- same-file priority summary carry-forward: `Tenant Plans + Fee Ledger stubs`

Q6 classification result:

- current Q6 label ownership is static, descriptive, and file-local
- current Q6 label ownership is not runtime-rendered under the same wording
- current Q6 label ownership is not derived from API payloads or database state as a visible string
- current Q6 label ownership is not shared across multiple runtime surfaces

Therefore:

- the exact current Q6 implementation boundary is one descriptive matrix file, not a runtime file set
- live runtime plan-related labels are neighboring analogs only and must not be pulled into Q6 by
  implication

## 6. Adjacent-Surface Coupling Decision

Must change together in a later isolated Q6-only pass:

- the Finance Tower row label in `docs/DASHBOARD_MATRIX_CONTROL_TENANT_WL.md`
- the same-file priority summary reference in `docs/DASHBOARD_MATRIX_CONTROL_TENANT_WL.md`

Do not need concurrent change for Q6 truthfulness:

- `Fee Ledger`, `Transaction Fees`, and `Settlement Status` in the same Finance Tower block
- `Plan & Quotas` and `Billing` in `components/ControlPlane/TenantDetails.tsx`
- `Plan` in `components/ControlPlane/TenantRegistry.tsx`
- `Tenants` and `Finance & Fees` in `layouts/SuperAdminShell.tsx`

Reason:

- those labels are either separate finance concepts or separate runtime surfaces with different
  wording and different ownership boundaries
- changing them together would widen Q6 from an exact-label correction into a new runtime
  normalization pass

## 7. Smallest Safe Future Implementation Allowlist

For a strict Q6-only implementation pass that corrects the exact current visible label now present
in the repo, the smallest safe modify allowlist is:

- `docs/DASHBOARD_MATRIX_CONTROL_TENANT_WL.md`

Permitted change shape inside that single file:

- rename `Tenant Plans` to `Tenant plan metadata`
- keep Finance Tower placement intact
- optionally add a short package-visibility or billing-visibility clarifier if needed
- update the same-file priority summary reference so it stays aligned with the renamed row

Explicitly excluded from that later isolated allowlist:

- `components/ControlPlane/TenantDetails.tsx`
- `components/ControlPlane/TenantRegistry.tsx`
- `layouts/SuperAdminShell.tsx`
- `App.tsx`
- `services/controlPlaneService.ts`
- `types.ts`
- `server/src/services/tenantProvision.service.ts`

Widening trigger:

- if the user later wants a live runtime label introduced or normalized to match the product-truth
  phrase, that is a separate bounded pass and not a Q6 exact-label correction

## 8. Blockers and Edge Cases

- no blocker prevents an isolated Q6 correction at the exact current source surface because the
  label is file-local and static
- there is no exact live runtime owner for `Tenant Plans`, so a runtime-targeting request under the
  same wording would currently have no exact match to edit
- backend and data-layer files already carry `plan` metadata, which means absence of a live runtime
  `Tenant Plans` label is a presentation-scope fact rather than a data-availability blocker

## 9. Readiness Outcome

Result of this artifact:

- TexQtic now has one bounded Q6 file-targeting plan that identifies the exact current label owner
  without widening into runtime or architecture work
- Q6 remains safe for isolated first-wave handling because its exact current source is one
  descriptive matrix file
- the later Q6-only implementation allowlist is reduced to one file, with one same-file summary
  alignment requirement
- any live runtime label introduction remains explicitly out of scope for Q6 and requires a new
  bounded pass
