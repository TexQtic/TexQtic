# TEXQTIC-UNDER-ARTIFACTED-CONTROL-FAMILY-REGISTER-v1

## Status

Compact under-artifacted control-family register for formal-audit preparation only.

This register exists so the later audit can classify these areas without overreading or losing
them.

## 1. AI Governance / Advisory Automation

### Family / area

AI Governance / Advisory Automation.

### Current real surface

- `components/ControlPlane/AiGovernance.tsx` provides bounded control-plane AI insights and AI
  budget visibility
- `docs/strategy/PLATFORM_DOCTRINE_ADDENDUM.md` fixes advisory-only AI posture and the future gates
  on automation

### Why authority is currently too weak

There is no standalone current product-truth family authority for AI governance. Current truth is
split between runtime evidence, doctrinal gating, and descriptive control-plane grouping.

### What formalization is still missing

- a first-class current family note separating bounded present control-plane truth from broader
  advisory automation

### How the later audit should avoid over- or under-reading it

- do not overread the runtime surface as autonomous AI completion
- do not underread it as nonexistent because doctrine remains restrictive
- classify it as a real bounded control-family area with future automation still gated

## 2. Feature Governance / Release Controls

### Family / area

Feature Governance / Release Controls / Kill-Switches.

### Current real surface

- `components/ControlPlane/FeatureFlags.tsx` provides real flag and kill-switch control
- `docs/strategy/CONTROL_CENTER_TAXONOMY.md` describes these controls as implemented platform
  operator surfaces

### Why authority is currently too weak

There is runtime truth and descriptive placement, but no explicit current product-truth family note
for this bounded control family.

### What formalization is still missing

- a family clarification stating present runtime scope, excluded broader release-governance scope,
  and placement inside platform control-plane

### How the later audit should avoid over- or under-reading it

- do not lose this family because it lacks a first-class anchor today
- do not overread it as a complete release-management program
- classify it as a real bounded control-plane subfamily with thinner policy depth

## 3. Billing / Commercial Admin Operations

### Family / area

Billing / Tenant Billing / Commercial Admin Operations.

### Current real surface

- `components/ControlPlane/FinanceOps.tsx` provides real bounded finance supervision and casework
- `docs/strategy/CONTROL_CENTER_TAXONOMY.md` distinguishes implemented finance supervision from thin
  or future tenant-billing and reconciliation depth

### Why authority is currently too weak

The real supervision surface exists, but the broader billing and commercial-admin family remains
under-formalized and is easy to blur with subscription, payments, or finance-state families.

### What formalization is still missing

- a family clarification separating current supervision from tenant billing, reconciliation, and
  broader commercial-admin thinness

### How the later audit should avoid over- or under-reading it

- do not overread Finance Tower language as a complete mature billing family
- do not underread the real finance-supervision surface
- classify this area as a real but under-formalized control/commercial-admin family with thinner
  operational depth than its descriptive grouping may suggest
