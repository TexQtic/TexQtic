# TEXQTIC — TAXONOMY AND NAMING DESCENDANT RECONCILIATION — 2026-04-09

## Scope

Bounded downstream governance taxonomy and naming descendant reconciliation only.

This pass was limited to downstream governance descendants that still presented stale
enterprise-as-runtime or white-label-as-parent-mode wording as current guidance after the
opening-layer taxonomy reset.

This pass did **not** reopen authority-routing work, Layer 0 canon, technical contract parity,
identity/tenancy continuity, onboarding/control-plane planning, downstream family design
authorities, delivery regeneration, debt cleanup, or broad historical document rewrite.

## Opening-Layer Inputs Re-read

1. `governance/control/TEXQTIC-OPENING-LAYER-TAXONOMY-TRUTH-BASELINE-2026-04-09.md`
2. `governance/control/TEXQTIC-OPENING-LAYER-SEQUENCING-AND-NEXT-CYCLE-ENTRY-2026-04-09.md`
3. `governance/analysis/TEXQTIC-GOVERNANCE-ALIGNMENT-PLAN-FROM-REPO-TRUTH-2026-04-09.md`
4. `governance/analysis/TEXQTIC-FINAL-LIVE-GOVERNANCE-AUTHORITY-DESCENDANT-SWEEP-2026-04-09.md`

## Taxonomy Baseline Applied

1. `enterprise` is not a separate runtime family; when used as current runtime guidance it must be
   requalified to B2B workspace depth or commercial-plan language.
2. `white-label` is overlay capability and deployment/runtime-path truth, not a parent runtime
   family or parent commercial mode.
3. `b2b_workspace`, `b2c_storefront`, `wl_storefront` with optional WL admin overlay,
   `aggregator_workspace`, and `control_plane` remain the opening-layer runtime families.
4. `app.org_id` remains canonical and `app.tenant_id` remains stale, but no non-historical unit or
   decision descendant in the inspected target set still treated `app.tenant_id` as current
   guidance.
5. `blocked_pending_verification` remains a reporting/governance label, but no non-historical unit
   or decision descendant in the inspected target set still presented it as a separate runtime
   category.

## Candidate Classification

### Reconciled Now

1. `governance/units/TECS-RUNTIME-VERIFICATION-HARDENING-001.md`
   - stale descendant issue: current unit title, acceptance language, and confirmation wording used
     `tenant-enterprise` as a runtime-category label and used white-label as an unqualified peer
     slice.
   - bounded reconciliation: reframed the active runtime guidance to `B2B workspace` plus
     `white-label overlay` wording while preserving the historical implementation commit label.

2. `governance/decisions/GOV-DEC-RUNTIME-VERIFICATION-HARDENING-OPENING.md`
   - stale descendant issue: opening decision still authorized one unit using
     `tenant-enterprise and white-label slices` language.
   - bounded reconciliation: requalified the opening to `B2B workspace and white-label overlay`
     slices and aligned the in-scope verification bullets to current runtime taxonomy.

3. `governance/units/REALM-BOUNDARY-SHELL-AFFORDANCE-001.md`
   - stale descendant issue: the current unit record still stated `enterprise tenant shell` and
     `white-label tenant shell` as if they were peer runtime-category labels.
   - bounded reconciliation: requalified the affected runtime surfaces to `B2B workspace` and
     `white-label overlay` shell language without widening the defect or changing preserved
     deployment lineage.

4. `governance/units/SUBSCRIPTION-FRONTEND-CANONICAL-VOCABULARY-ALIGNMENT.md`
   - stale descendant issue: runtime validation lines still described the exercised surfaces as
     `enterprise tenant surfaces` and `white-label tenant surfaces` even though the unit itself is
     about commercial-plan vocabulary, not parent runtime taxonomy.
   - bounded reconciliation: requalified those verification surfaces to `B2B workspace` and
     `white-label overlay` wording while preserving plan-tier `ENTERPRISE` as valid commercial
     vocabulary.

### Preserved And Not Rewritten In This Pass

1. Historical audits, trackers, wave logs, archive records, temp analyses, and legacy master-plan
   surfaces.
2. Decision or evidence files that mention `enterprise` or `white-label` only as exact observed
   runtime history rather than current descendant guidance.
3. Family-level design anchors and later-cycle planning surfaces that require their own bounded
   descendant reconciliation rather than ad hoc wording edits here.

## Exact Changes Applied

1. Replaced stale runtime-category labels with current opening-layer terminology in the minimal
   descendant set above.
2. Preserved historical implementation commit identifiers and older commit-message strings where
   they function only as lineage evidence.
3. Left commercial-plan vocabulary such as `ENTERPRISE` unchanged where it is legitimately plan
   truth rather than runtime-family truth.

## Risks And Guardrails

1. This pass does not claim that every historical occurrence of `enterprise` or `white-label` in
   governance history has been rewritten.
2. Exact observed production-evidence wording was preserved where rewriting would collapse lineage
   or broaden this pass into historical cleanup.
3. Later descendant passes may still need to reconcile onboarding/system-design, family-level, or
   delivery-planning surfaces that discuss taxonomy at broader architectural scope.

## Completion State

This bounded taxonomy and naming descendant pass is complete for the minimal downstream governance
descendants that still used stale enterprise/white-label runtime labels as current guidance.

No further non-historical unit or opening-decision descendant was found in the inspected bounded
set that still treated `app.tenant_id` as canonical, treated `blocked_pending_verification` as a
runtime family, or required additional taxonomy/naming correction without widening scope.
